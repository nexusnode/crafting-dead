package com.craftingdead.survival.client.renderer.entity.layers;

/*
 * @OnlyIn(Dist.CLIENT) public class ParachuteLayer <T extends PlayerEntity> extends
 * LayerRenderer<T, PlayerModel<T>> { private final ParachuteModel parachuteModel = new
 * ParachuteModel();
 * 
 * public ParachuteLayer(IEntityRenderer<T, PlayerModel<T>> entityRendererIn) {
 * super(entityRendererIn); }
 * 
 * @Override public void render(MatrixStack matrixStackIn, IRenderTypeBuffer bufferIn, int
 * packedLightIn, T entitylivingbaseIn, float limbSwing, float limbSwingAmount, float partialTicks,
 * float ageInTicks, float netHeadYaw, float headPitch) { ParachuteModel } }
 */
